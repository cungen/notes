---
title: 
draft: false
tags:
  - Area/AI/RL
---
## 双深度Q网络

**DQN问题**：Q 值总是被高估了，原因是目标的值很容易被设得太高

在双深度Q网络DDQN里面，选动作的Q函数与计算值的Q函数不是同一个
$$
Q(s_t,a_t)\leftrightarrow r_t+Q'\left(s_{t+1},arg\max_a Q(s_{t+1},a)\right)
$$

假设我们有两个Q函数————Q 和 Q′，如果 Q 高估了它选出来的动作 a，只要 Q′ 没有高估动作 a 的值，算出来的就还是正常的值。假设 Q′ 高估了某一个动作的值，也是没问题的，因为只要 Q 不选这个动作就可以，这就是DDQN神奇的地方

## 竞争深度Q网络

竞争深度Q网络（dueling DQN）改变了网络的架构。原来的深度Q网络直接输出 Q 值，竞争深度Q网络是分成两条路径运算。第一条路径会输出一个标量 V(s)，因为它与输入 s 是有关系的，所以称为 V(s)。第二条路径会输出一个向量 A(s,a)，它的每一个动作都有一个值。我们再把 V(s) 和A(s,a) 加起来就可以得到 Q 值 Q(s,a)。

为了防止A(s,a)学到所有的Q值，而V为0，一般会为A(s,a)添加限制，让所有动作的该值和为0，增加该值的学习难度。

**直观理解**：把环境和动作的价值分开学习，但一次性输出。

## 优先级经验回放

**优先级经验回放**（prioritized experience replay，PER）：根据经验数据的学习难度（时序差分误差大的，表示网络的输出与目标之间的差距） ，难度大的提高其采样概率，这样在训练的时候才会多考虑那些不好训练的数据。实际上在做 PER 的时候，我们不仅会更改采样的过程，还会因为更改了采样的过程，而更改更新参数的方法。所以PER不仅改变了采样数据的分布，还改变了训练过程。

## 在蒙特卡洛方法和时序差分方法中取得平衡

简单理解：就是执行n步后，分别计算开始和终止的Q值，然后看差值是否与真实回报一致。

## 噪声网络

噪声网络（noisy net）是对探索过程的改进。添加噪声的作用是让网络学习到关键特征。

ε-贪心这样的探索就是在动作的空间上加噪声，但存在问题是这样探索出的轨迹并不符合真实的网络输出的情况。

噪声网络是在整个Q网络的参数和权重上添加噪声，然后用添加完噪声的$\tilde{Q}$网络去与环境交互，并完成整个回合的探索。

## 分布式Q函数

**分布式Q函数**（distributional Q-function）：比较合理，但难以实现。

**直观理解**：Q值是累积奖励的期望值（计算时可能会取多次累积奖励的平均值）。但不同的分步，可以有同样平均值，所以如果单纯取均值可能会丢失一些信息。

计算方法：把Q函数输出期望的值，改为输出一个分布（$\sum_{q\in Q_a}p(q)*q$，其概率和为1，其期望值是Q值），如果有多个动作，就输出多个这样的分布。好处是分布的方差可以表示某个动作的风险评估。

## 彩虹

把上述全部方法都用上，再加上使用异步优势演员-评论员（asynchronous advantage actor-critic，A3C）方法。